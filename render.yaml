services:
  - type: web
    name: supplysense-backend
    env: node
    rootDir: ".sixth copy"
    branch: main
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
