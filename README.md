
  # Supply Chain Delay Prediction

  This is a code bundle for Supply Chain Delay Prediction. The original project is available at https://www.figma.com/design/JXljeotOMIwEundA1996Yg/Supply-Chain-Delay-Prediction.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  